<%= form_for(@chapter) do |f| %>
  <% if @chapter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chapter.errors.count, "error") %> prohibited this chapter from being saved:</h2>

      <ul>
      <% @chapter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>

  <%= f.fields_for :plot do |p| %>

    <%= p.fields_for(Plot.create) do |pa| %>
      <div class="field">    
        <%= pa.label :content %><br>
        <%= pa.text_field :content %>
       
      </div> 
    <% end %>

    <%= p.fields_for(Plot.create)) do |pa| %>
      <div class="field">
        <%= pa.label :content %><br>
        <%= pa.text_field :content %>
        

      </div> 
    <% end %>
    
  <% end %>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
