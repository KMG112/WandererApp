<ul>
  <% @chapters.each do |chapter| %>
    <li>
      <%= chapter.content %>
      <%= link_to "Delete", list_path(@list),:data => {:confirm => "You sure?",:method => "delete"} %>
    </li>
  <% end %>
</ul>